{
    "name": "PanelGroup",
    "version": "0.1.0",
    "description": "CAN sub-node domain layer for OpenSkyhawk panel boards — output dispatch, input polling, heartbeat",
    "keywords": ["CAN", "STM32", "OpenSkyhawk"],
    "frameworks": ["arduino"],
    "platforms": ["ststm32"],
    "dependencies": {
        "STM32Board": "*",
        "CANProtocol": "*"
    }
}
