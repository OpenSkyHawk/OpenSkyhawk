{
    "name": "PanelBridge",
    "version": "0.1.0",
    "description": "CAN master / UART bridge domain layer for OpenSkyhawk — forwards ControlPackets between RP2040 and CAN sub-nodes",
    "keywords": ["CAN", "STM32", "OpenSkyhawk"],
    "frameworks": ["arduino"],
    "platforms": ["ststm32"],
    "dependencies": {
        "STM32Board": "*",
        "CANProtocol": "*"
    }
}
