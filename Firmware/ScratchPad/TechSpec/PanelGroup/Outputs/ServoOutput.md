# ServoOutput — superseded

**Status:** Superseded by `NeedleGauge` (#122).

This output type is now a **constructor-selected backend** of `OpenSkyhawk::NeedleGauge`, not a
standalone class. See `NeedleGauge.md` and GitHub #122.
