# 3D model / CAD export assets — generated, not hand-edited.
# Mark binary so `git diff --check` (whitespace) and textual diffs skip them.
*.STEP binary
*.step binary
*.stp  binary
*.STP  binary
*.stl  binary
*.STL  binary
*.wrl  binary
*.WRL  binary
